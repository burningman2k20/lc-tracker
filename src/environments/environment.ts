import { initializeApp } from "@angular/fire/app";

// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBiGSF9MKf1WaZNN_pqH7WQyg5abwFpJes",
    authDomain: "lctracker-77029.firebaseapp.com",
    projectId: "lctracker-77029",
    storageBucket: "lctracker-77029.appspot.com",
    messagingSenderId: "479008989940",
    appId: "1:479008989940:web:89a07b767c8ab9ef7734a1"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
